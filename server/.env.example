# Server environment variables
# Copy to .env and fill in values — never commit .env

PORT=3001
NODE_ENV=development

# SQLite database path (defaults to ./data/occupy.db)
# DB_PATH=/var/data/occupy.db

# Allowed client origin for CORS (dev: Vite dev server)
CLIENT_ORIGIN=http://localhost:5173
