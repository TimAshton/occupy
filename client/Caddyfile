occupy.tashton.com {
    handle /api/* {
        reverse_proxy server:3001
    }

    handle /socket.io/* {
        reverse_proxy server:3001
    }

    handle {
        root * /usr/share/caddy
        file_server
        try_files {path} /index.html
    }
}
